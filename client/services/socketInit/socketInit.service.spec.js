'use strict';

describe('Service: socketInit', function () {

  // load the service's module
  beforeEach(module('baseApp'));

  // instantiate service
  var socketInit;
  beforeEach(inject(function (_socketInit_) {
    socketInit = _socketInit_;
  }));

  it('should do something', function () {
  });

});
