div(ng-include='"components/navbar/navbar.html"')
.jumbotron
  h1 Join Now! She's Waiting! It's Free!

  .row
    .col-sm-4
      p
        span.text-success No Credit Card Needed!
        br
        span.text-info We'll Even Give You Some House Money To Start With
      form.form(name='form', ng-submit='register(form)', novalidate='')
        .form-group(ng-class='{ "has-success": form.username.$valid && submitted,\
            "has-error": form.username.$invalid && submitted }')
          input.form-control(type='text', name='username', ng-model='newuser.username', required='', placeholder="Pick a Username")
          p.help-block(ng-show='form.username.$error.required && submitted')
            | A username is required

        .form-group(ng-class='{ "has-success": form.email.$valid && submitted,\
            "has-error": form.email.$invalid && submitted }')
          input.form-control(type='email', name='email', ng-model='newuser.email', required='', mongoose-error='', placeholder="Tell Us Your Email Address")
          p.help-block(ng-show='form.email.$error.email && submitted')
            | Doesn't look like a valid email.
          p.help-block(ng-show='form.email.$error.required && submitted')
            | What's your email address?
          p.help-block(ng-show='form.email.$error.mongoose')
            | {{ errors.email }}

        .form-group(ng-class='{ "has-success": form.password.$valid && submitted,\
            "has-error": form.password.$invalid && submitted }')
          input.form-control(type='password', name='password', ng-model='newuser.password', ng-minlength='3', required='', mongoose-error='', placeholder="Pick A Password")
          p.help-block(ng-show='(form.password.$error.minlength || form.password.$error.required) && submitted')
            | Password must be at least 3 characters.
          p.help-block(ng-show='form.password.$error.mongoose')
            | {{ errors.password }}

        div
          button.btn.btn-inverse.btn-lg.btn-login(type='submit')
            | Sign up
          = ' '
          a.btn.btn-default.btn-lg.btn-register(href='/login')
            | Login
.contain-main
  div(ng-include='"components/broadcasterSuggestions/broadcasterSuggestions.html"')
